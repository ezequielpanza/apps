name: Hotfix Deploy Wander v0.106.0

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/wander-travel/**"
      - ".github/workflows/hotfix-deploy-wander-v0106.yml"

permissions:
  contents: read

concurrency:
  group: hotfix-deploy-wander-v0106
  cancel-in-progress: true

jobs:
  deploy:
    if: github.head_ref == 'hotfix/wander-live-v0.106.0'
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify v0.106.0 shell
        run: |
          node apps/wander-travel/tests/app-shell.mjs
          find apps/wander-travel -maxdepth 1 -type f -name '*.js' -print0 | xargs -0 -n1 node --check

      - name: Publish v0.106.0 to Cloudflare Pages
        working-directory: apps/wander-travel
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@latest pages deploy . --project-name=wander-travel --branch=main

      - name: Verify live version
        run: |
          version_file="${RUNNER_TEMP}/wander-runtime-version.js"
          manifest_file="${RUNNER_TEMP}/wander-manifest.json"
          for attempt in 1 2 3 4 5 6 7 8; do
            nonce="${GITHUB_SHA}-${attempt}-$(date +%s)"
            curl --fail --silent --show-error --location --header 'Cache-Control: no-cache' \
              "https://wander-travel.pages.dev/runtime-version.js?probe=${nonce}" -o "${version_file}" || true
            curl --fail --silent --show-error --location --header 'Cache-Control: no-cache' \
              "https://wander-travel.pages.dev/manifest.webmanifest?probe=${nonce}" -o "${manifest_file}" || true
            if grep -q "const VERSION = 'v0.106.0'" "${version_file}" 2>/dev/null \
              && grep -q '"start_url": "./?app=v0.106.0"' "${manifest_file}" 2>/dev/null; then
              echo "Wander v0.106.0 is live."
              cat "${version_file}"
              exit 0
            fi
            echo "Live verification attempt ${attempt} still returned an older version."
            sleep 12
          done
          echo "runtime-version.js returned:"
          cat "${version_file}" || true
          echo "manifest.webmanifest returned:"
          cat "${manifest_file}" || true
          exit 1
