name: Apply Wander Raw Track Recording

on:
  push:
    branches: [feature/wander-raw-track-recording]
    paths:
      - "scripts/apply-wander-raw-track-recording.py"
      - ".github/workflows/apply-wander-raw-track-recording.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/wander-raw-track-recording
          fetch-depth: 0

      - name: Apply raw tracking architecture
        run: python scripts/apply-wander-raw-track-recording.py

      - name: Remove temporary migration files
        run: |
          rm scripts/apply-wander-raw-track-recording.py
          rm .github/workflows/apply-wander-raw-track-recording.yml

      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Implement raw track recording and optional visual smoothing"
          git push origin HEAD:feature/wander-raw-track-recording
