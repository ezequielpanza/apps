import {createGpsModule} from './modules/gps.js';
import {createBatteriesModule} from './modules/batteries.js';
import {createPhoneModule} from './modules/phone.js';
import {createSeaStateModule} from './modules/seastate.js';
import {createCompassModule} from './modules/compass.js';

const cards=document.getElementById('cards');
const menu=document.getElementById('menuSheet');
const add=document.getElementById('addSheet');
const ALL_MODULE_IDS=['gps','batteries','phone','seastate','compass'];
const readJson=(key,fallback)=>{try{const v=JSON.parse(localStorage.getItem(key)||'null');return v??fallback}catch(_){return fallback}};
const savedOrder=readJson('bs.ui.order',readJson('bs.clean.order',null));
const ui={order:Array.isArray(savedOrder)?savedOrder.filter(id=>ALL_MODULE_IDS.includes(id)):[...ALL_MODULE_IDS],collapsed:{},page:{},heights:readJson('bs.ui.heights',readJson('bs.clean.heights',{}))};
const modules={};
let lockedScrollY=0,sheetDrag=null,moduleMenuId=null;
let reorder=null,resize=null,tapCandidate=null,touchSwipe=null,lastTouchSwipeAt=0,refreshGesture=null;

function requestRender(id){renderModule(id)}
function openBatteryManager(){renderBatteryManager();openSheet(batteryManager)}
modules.gps=createGpsModule(requestRender);
modules.batteries=createBatteriesModule(requestRender,openBatteryManager);
modules.phone=createPhoneModule(requestRender);
modules.seastate=createSeaStateModule(requestRender);
modules.compass=createCompassModule(requestRender);

function pager(count,current){return `<div class="pager">${Array.from({length:count},(_,i)=>`<span class="${i===current?'on':''}">●</span>`).join('')}</div>`}
function updatePager(card,current){card.querySelectorAll('.pager').forEach(p=>p.querySelectorAll('span').forEach((dot,i)=>dot.classList.toggle('on',i===current)))}
function dragHandle(){return '<button class="drag-handle" type="button" aria-label="Mover módulo"><i></i><i></i><i></i><i></i><i></i><i></i></button>'}
function resizeHandle(){return '<button class="resize-handle" type="button" aria-label="Cambiar tamaño del módulo"><i></i><i></i><i></i></button>'}
function moduleHtml(id){const m=modules[id],p=Math.min(ui.page[id]||0,m.pages-1);const pages=Array.from({length:m.pages},(_,i)=>`<div class="page" data-page="${i}">${m.page(i)}${pager(m.pages,p)}</div>`).join('');return `<section class="card${ui.collapsed[id]?' collapsed':''}" data-id="${id}"><header class="card-head">${dragHandle()}<span class="dot"></span><span class="title">${m.name}</span><span class="summary">${m.summary()}</span><button class="more" type="button">⋮</button></header><div class="card-body"><div class="viewport"><div class="track" style="transform:translateX(-${p*100}%)">${pages}</div></div></div>${resizeHandle()}</section>`}
function saveOrder(){localStorage.setItem('bs.ui.order',JSON.stringify(ui.order))}
function saveHeights(){localStorage.setItem('bs.ui.heights',JSON.stringify(ui.heights))}
function pageHeight(id,page){const stored=ui.heights[id];if(stored&&typeof stored==='object'&&Number.isFinite(Number(stored[page])))return Number(stored[page]);if(Number.isFinite(Number(stored))&&page===0)return Number(stored);return null}
function setPageHeight(id,page,height){const current=ui.heights[id]&&typeof ui.heights[id]==='object'?ui.heights[id]:{};ui.heights[id]={...current,[page]:Math.round(height)}}
function measure(card){if(!card||card.classList.contains('collapsed'))return;const id=card.dataset.id,body=card.querySelector('.card-body');if(!body)return;const p=Math.min(ui.page[id]||0,modules[id].pages-1),fixed=pageHeight(id,p);if(fixed!==null){body.style.height=`${fixed}px`;return}const page=card.querySelector(`.page[data-page="${p}"]`);if(page)body.style.height=page.scrollHeight+'px'}
function hydrate(card){const id=card.dataset.id;modules[id].afterRender?.(card);updatePager(card,ui.page[id]||0);requestAnimationFrame(()=>measure(card))}
function renderAll(){cards.innerHTML=ui.order.map(moduleHtml).join('');cards.querySelectorAll('.card').forEach(hydrate)}
function renderModule(id){const old=cards.querySelector(`.card[data-id="${id}"]`);if(!old)return;const holder=document.createElement('div');holder.innerHTML=moduleHtml(id);old.replaceWith(holder.firstElementChild);hydrate(cards.querySelector(`.card[data-id="${id}"]`))}
function setModulePage(card,next){if(!card)return;const id=card.dataset.id,m=modules[id];if(!m||m.pages<2)return;const page=Math.max(0,Math.min(m.pages-1,next));if(page===(ui.page[id]||0))return;ui.page[id]=page;card.querySelector('.track').style.transform=`translateX(-${page*100}%)`;updatePager(card,page);setTimeout(()=>{measure(card);m.afterRender?.(card)},190)}

function anySheetOpen(){return [...document.querySelectorAll('.sheet')].some(s=>s.classList.contains('open'))}
function lockModuleScroll(){if(document.body.classList.contains('menu-open'))return;lockedScrollY=window.scrollY||document.documentElement.scrollTop||0;document.body.classList.add('menu-open');document.body.style.top=`-${lockedScrollY}px`}
function unlockModuleScroll(){if(anySheetOpen()||!document.body.classList.contains('menu-open'))return;document.body.classList.remove('menu-open');document.body.style.top='';window.scrollTo(0,lockedScrollY)}
function openSheet(sheet){const inner=sheet.querySelector('.sheet-inner');if(inner){inner.style.transform='';inner.style.transition=''}sheet.classList.add('open');lockModuleScroll()}
function closeSheet(sheet){const inner=sheet.querySelector('.sheet-inner');if(inner){inner.style.transform='';inner.style.transition=''}sheet.classList.remove('open');unlockModuleScroll()}
function bindSheetHandle(sheet){const handle=sheet.querySelector('.handle'),inner=sheet.querySelector('.sheet-inner');if(!handle||!inner||handle.dataset.bound)return;handle.dataset.bound='1';handle.addEventListener('pointerdown',e=>{if(!sheet.classList.contains('open')||sheet.classList.contains('fullscreen-sheet'))return;e.preventDefault();e.stopPropagation();sheetDrag={sheet,inner,startY:e.clientY,pointerId:e.pointerId};inner.style.transition='none';try{handle.setPointerCapture(e.pointerId)}catch(_){}})}
function makeSheet(full=false){const s=document.createElement('div');s.className='sheet'+(full?' fullscreen-sheet':'');document.body.appendChild(s);return s}
function fullHeader(title,backAttr='data-full-back'){return `<div class="fullscreen-head"><button class="fullscreen-back" type="button" ${backAttr}>‹</button><h3>${title}</h3></div>`}

const moduleMenu=makeSheet(false),unitsMenu=makeSheet(true),coordMenu=makeSheet(true),batteryManager=makeSheet(true),bluetoothScanner=makeSheet(true);let scanDevices=[];
function renderModuleMenu(id){const isGps=id==='gps',isBat=id==='batteries';moduleMenu.innerHTML=`<div class="sheet-inner compact-sheet"><div class="handle"></div><h3>${modules[id].name}</h3>${isGps?'<button class="option sheet-option" type="button" data-gps-units>Cambiar unidades</button><button class="option sheet-option" type="button" data-gps-coords>Formato de coordenadas</button>':''}${isBat?'<button class="option sheet-option" type="button" data-battery-manage-open>Administrar Banco de Baterías</button>':''}<button class="option sheet-option danger" type="button" data-module-delete>Eliminar</button></div>`;bindSheetHandle(moduleMenu)}
function openModuleMenu(id){moduleMenuId=id;renderModuleMenu(id);openSheet(moduleMenu)}
function renderUnitsMenu(){const gps=modules.gps,current=gps.getUnits();unitsMenu.innerHTML=`<div class="sheet-inner fullscreen-inner">${fullHeader('Unidades GPS','data-units-back')}${gps.getUnitOptions().map(o=>`<button class="add-module-row" type="button" data-unit="${o.id}"><span>${o.label}</span><span class="add-module-state ${o.id===current?'present':''}">${o.id===current?'✓':''}</span></button>`).join('')}</div>`}
function openUnitsMenu(){closeSheet(moduleMenu);renderUnitsMenu();openSheet(unitsMenu)}
function renderCoordMenu(){const gps=modules.gps,current=gps.getCoordFormat();coordMenu.innerHTML=`<div class="sheet-inner fullscreen-inner">${fullHeader('Formato de coordenadas','data-coord-back')}${gps.getCoordFormatOptions().map(o=>`<button class="add-module-row" type="button" data-coord-format="${o.id}"><span>${o.label}</span><span class="add-module-state ${o.id===current?'present':''}">${o.id===current?'✓':''}</span></button>`).join('')}</div>`}
function openCoordMenu(){closeSheet(moduleMenu);renderCoordMenu();openSheet(coordMenu)}
function deleteModule(id){if(!id)return;ui.order=ui.order.filter(x=>x!==id);delete ui.collapsed[id];delete ui.page[id];saveOrder();closeSheet(moduleMenu);moduleMenuId=null;renderAll()}
function renderAddSheet(){const inner=add.querySelector('.sheet-inner');if(!inner)return;inner.innerHTML='<div class="handle"></div><h3>Agregar módulo</h3>'+ALL_MODULE_IDS.map(id=>{const present=ui.order.includes(id);return `<button class="add-module-row" type="button" data-add-module="${id}" ${present?'disabled':''}><span>${modules[id].name}</span><span class="add-module-state ${present?'present':''}">${present?'✓':'+'}</span></button>`}).join('');bindSheetHandle(add)}
function renderBatteryManager(){const b=modules.batteries.state,s=modules.batteries.stats();batteryManager.innerHTML=`<div class="sheet-inner fullscreen-inner">${fullHeader('Banco de baterías','data-battery-manager-back')}<div class="full-section"><label class="full-label">Nombre del banco</label><input class="full-input" data-bank-name value="${String(b.bankName||'').replace(/"/g,'&quot;')}"><div class="full-label">Capacidad del banco</div><div class="full-readonly"><strong>${Math.round(s.cap)} Ah</strong><small>Calculada automáticamente según las baterías vinculadas</small></div></div><div class="full-section"><div class="full-section-title">Baterías vinculadas</div>${b.batteries.length?b.batteries.map(x=>`<div class="battery-manage-row"><div><strong>${x.name||x.deviceName||'Batería'}</strong><small>${Number(x.capacityAh)||0} Ah${x.address||x.id?` · ${x.address||x.id}`:''}</small></div><button class="mini-danger" type="button" data-remove-battery="${x.id}">Eliminar</button></div>`).join(''):'<div class="subtle">No hay baterías vinculadas.</div>'}<button class="primary-full-button" type="button" data-open-scanner>Vincular batería</button></div></div>`}
function renderBluetoothScanner(status='Escaneando dispositivos Bluetooth…'){bluetoothScanner.innerHTML=`<div class="sheet-inner fullscreen-inner">${fullHeader('Bluetooth','data-scanner-back')}<div class="scanner-status">${status}</div><div class="scanner-list">${scanDevices.length?scanDevices.map((d,i)=>`<button class="scanner-device" type="button" data-scan-device="${i}"><span><strong>${d.name||d.deviceName||'Dispositivo Bluetooth'}</strong><small>${d.address||d.id||d.mac||''}</small></span><span class="add-module-state">+</span></button>`).join(''):'<div class="scanner-empty">Los dispositivos aparecerán acá cuando el Core los detecte.</div>'}</div></div>`}
function requestBluetoothScan(){scanDevices=[];renderBluetoothScanner();openSheet(bluetoothScanner);let called=false;try{if(window.BoatStationCore?.openBluetoothScanner){window.BoatStationCore.openBluetoothScanner();called=true}else if(window.NativeBridge?.startBatteryScan){window.NativeBridge.startBatteryScan();called=true}}catch(_){}if(!called)renderBluetoothScanner('Esperando acceso Bluetooth del Core…')}

renderAll();
document.getElementById('menuBtn').onclick=()=>openSheet(menu);document.getElementById('addBtn').onclick=()=>{renderAddSheet();openSheet(add)};
[menu,add,moduleMenu].forEach(s=>s.addEventListener('click',e=>{if(e.target===s)closeSheet(s)}));
moduleMenu.addEventListener('click',e=>{if(e.target.closest('[data-module-delete]')){e.preventDefault();deleteModule(moduleMenuId)}else if(e.target.closest('[data-gps-units]')){e.preventDefault();openUnitsMenu()}else if(e.target.closest('[data-gps-coords]')){e.preventDefault();openCoordMenu()}else if(e.target.closest('[data-battery-manage-open]')){e.preventDefault();closeSheet(moduleMenu);openBatteryManager()}});
unitsMenu.addEventListener('click',e=>{if(e.target.closest('[data-units-back]')){closeSheet(unitsMenu);openModuleMenu('gps');return}const btn=e.target.closest('[data-unit]');if(btn){modules.gps.setUnits(btn.dataset.unit);renderUnitsMenu()}});coordMenu.addEventListener('click',e=>{if(e.target.closest('[data-coord-back]')){closeSheet(coordMenu);openModuleMenu('gps');return}const btn=e.target.closest('[data-coord-format]');if(btn){modules.gps.setCoordFormat(btn.dataset.coordFormat);renderCoordMenu()}});add.addEventListener('click',e=>{const btn=e.target.closest('[data-add-module]');if(!btn||btn.disabled)return;const id=btn.dataset.addModule;if(!ui.order.includes(id)){ui.order.push(id);saveOrder();renderAll();renderAddSheet()}});batteryManager.addEventListener('click',e=>{if(e.target.closest('[data-battery-manager-back]')){closeSheet(batteryManager);return}if(e.target.closest('[data-open-scanner]')){closeSheet(batteryManager);requestBluetoothScan();return}const rm=e.target.closest('[data-remove-battery]');if(rm){modules.batteries.removeBattery(rm.dataset.removeBattery);renderBatteryManager()}});batteryManager.addEventListener('change',e=>{if(e.target.matches('[data-bank-name]'))modules.batteries.renameBank(e.target.value)});bluetoothScanner.addEventListener('click',e=>{if(e.target.closest('[data-scanner-back]')){closeSheet(bluetoothScanner);openBatteryManager();return}const btn=e.target.closest('[data-scan-device]');if(btn){const d=scanDevices[Number(btn.dataset.scanDevice)];if(d){modules.batteries.addBattery(d);closeSheet(bluetoothScanner);openBatteryManager()}}});[menu,add,moduleMenu].forEach(bindSheetHandle);

document.addEventListener('pointermove',e=>{if(!sheetDrag||e.pointerId!==sheetDrag.pointerId)return;e.preventDefault();sheetDrag.inner.style.transform=`translateY(${Math.max(0,e.clientY-sheetDrag.startY)}px)`},{passive:false});document.addEventListener('pointerup',e=>{if(!sheetDrag||e.pointerId!==sheetDrag.pointerId)return;const d=sheetDrag,dy=Math.max(0,e.clientY-d.startY);sheetDrag=null;d.inner.style.transition='transform .16s ease';if(dy>=72){d.inner.style.transform='translateY(110%)';setTimeout(()=>closeSheet(d.sheet),160)}else{d.inner.style.transform='translateY(0)';setTimeout(()=>{d.inner.style.transition='';d.inner.style.transform=''},160)}});document.addEventListener('pointercancel',()=>{if(!sheetDrag)return;const d=sheetDrag;sheetDrag=null;d.inner.style.transition='transform .16s ease';d.inner.style.transform='translateY(0)';setTimeout(()=>{d.inner.style.transition='';d.inner.style.transform=''},160)});
function finishReorder(){if(!reorder)return;reorder.card.classList.remove('reordering');ui.order=[...cards.querySelectorAll('.card')].map(c=>c.dataset.id);saveOrder();reorder=null;cards.querySelectorAll('.card').forEach(measure)}function finishResize(){if(!resize)return;resize.card.classList.remove('resizing');saveHeights();resize=null}function touchById(list,id){for(const t of list)if(t.identifier===id)return t;return null}
cards.addEventListener('pointerdown',e=>{if(document.body.classList.contains('menu-open'))return;const card=e.target.closest('.card');if(!card)return;const grip=e.target.closest('.resize-handle');if(grip){e.preventDefault();e.stopPropagation();const body=card.querySelector('.card-body'),id=card.dataset.id,page=Math.min(ui.page[id]||0,modules[id].pages-1);resize={card,body,id,page,startY:e.clientY,startH:body.getBoundingClientRect().height,pointerId:e.pointerId};card.classList.add('resizing');try{grip.setPointerCapture(e.pointerId)}catch(_){};return}const drag=e.target.closest('.drag-handle');if(drag){e.preventDefault();reorder={card,pointerId:e.pointerId};card.classList.add('reordering');try{drag.setPointerCapture(e.pointerId)}catch(_){};return}if(e.target.closest('button,input'))return;if(!e.target.closest('.card-head'))return;tapCandidate={id:card.dataset.id,card,x:e.clientX,y:e.clientY,t:Date.now(),pointerType:e.pointerType}});cards.addEventListener('pointermove',e=>{if(resize){e.preventDefault();const next=Math.max(70,Math.min(Math.max(120,window.innerHeight*.78),resize.startH+(e.clientY-resize.startY)));resize.body.style.height=`${next}px`;setPageHeight(resize.id,resize.page,next);return}if(reorder){e.preventDefault();const moving=reorder.card,others=[...cards.querySelectorAll('.card')].filter(c=>c!==moving);let before=null;for(const c of others){const r=c.getBoundingClientRect();if(e.clientY<r.top+r.height/2){before=c;break}}if(before)cards.insertBefore(moving,before);else cards.appendChild(moving);return}if(tapCandidate&&(Math.abs(e.clientX-tapCandidate.x)>10||Math.abs(e.clientY-tapCandidate.y)>10))tapCandidate=null});cards.addEventListener('pointerup',e=>{if(resize){finishResize();return}if(reorder){finishReorder();return}if(!tapCandidate)return;const g=tapCandidate;tapCandidate=null;if(g.pointerType==='touch'&&Date.now()-lastTouchSwipeAt<400)return;if(Math.abs(e.clientX-g.x)<8&&Math.abs(e.clientY-g.y)<8&&Date.now()-g.t<350){ui.collapsed[g.id]=!ui.collapsed[g.id];g.card.classList.toggle('collapsed',ui.collapsed[g.id]);if(!ui.collapsed[g.id])measure(g.card)}});cards.addEventListener('pointercancel',()=>{tapCandidate=null;finishResize();finishReorder()});cards.addEventListener('touchstart',e=>{if(document.body.classList.contains('menu-open')||e.touches.length!==1)return;if(e.target.closest('.card-head,.drag-handle,.resize-handle,button,input,textarea,select,a'))return;const card=e.target.closest('.card');if(!card||modules[card.dataset.id].pages<2)return;const t=e.touches[0];touchSwipe={id:t.identifier,card,moduleId:card.dataset.id,startX:t.clientX,startY:t.clientY,mode:'pending'}},{passive:true});cards.addEventListener('touchmove',e=>{if(!touchSwipe)return;const t=touchById(e.touches,touchSwipe.id);if(!t)return;const dx=t.clientX-touchSwipe.startX,dy=t.clientY-touchSwipe.startY;if(touchSwipe.mode==='pending'){if(Math.abs(dx)<10&&Math.abs(dy)<10)return;if(Math.abs(dx)>Math.abs(dy)*1.15)touchSwipe.mode='horizontal';else{touchSwipe=null;return}}if(touchSwipe?.mode==='horizontal'){e.preventDefault();refreshGesture=null;hideRefresh()}},{passive:false});cards.addEventListener('touchend',e=>{if(!touchSwipe)return;const t=touchById(e.changedTouches,touchSwipe.id);if(!t){touchSwipe=null;return}const g=touchSwipe;touchSwipe=null;if(g.mode!=='horizontal')return;const dx=t.clientX-g.startX,dy=t.clientY-g.startY;if(Math.abs(dx)<38||Math.abs(dx)<=Math.abs(dy)*1.1)return;const cur=ui.page[g.moduleId]||0;setModulePage(g.card,dx<0?cur+1:cur-1);lastTouchSwipeAt=Date.now()},{passive:true});cards.addEventListener('touchcancel',()=>{touchSwipe=null});cards.addEventListener('click',e=>{const more=e.target.closest('.more');if(more){e.stopPropagation();openModuleMenu(more.closest('.card').dataset.id)}});window.addEventListener('resize',()=>cards.querySelectorAll('.card').forEach(c=>{measure(c);modules[c.dataset.id].afterRender?.(c)}));
const deck=document.createElement('div');deck.className='module-deck';cards.parentNode.insertBefore(deck,cards);deck.appendChild(cards);const refreshTab=document.createElement('div');refreshTab.className='hidden-refresh-module';refreshTab.textContent='Actualizar';deck.insertBefore(refreshTab,cards);const REFRESH_DISTANCE=72,REFRESH_REVEAL=50,DIRECTION_LOCK=12;function atTop(){const root=document.scrollingElement||document.documentElement;return root.scrollTop<=1}function canStartRefresh(target){if(document.body.classList.contains('menu-open')||!atTop())return false;if(target.closest('.sheet,.drag-handle,.resize-handle,button,input,textarea,select,a'))return false;return true}function revealRefresh(distance){const progress=Math.min(1,distance/REFRESH_DISTANCE),armed=distance>=REFRESH_DISTANCE;deck.style.setProperty('--deck-pull',`${progress*REFRESH_REVEAL}px`);refreshTab.classList.toggle('armed',armed);deck.classList.add('pulling');return armed}function hideRefresh(){deck.classList.remove('pulling','refreshing');deck.style.removeProperty('--deck-pull');refreshTab.classList.remove('armed');refreshTab.textContent='Actualizar'}function beginRefreshGesture(touch,target){if(!canStartRefresh(target))return;refreshGesture={id:touch.identifier,startX:touch.clientX,startY:touch.clientY,armed:false,mode:'pending'}}function moveRefreshGesture(touch,event){if(!refreshGesture||touch.identifier!==refreshGesture.id)return;const dx=touch.clientX-refreshGesture.startX,dy=touch.clientY-refreshGesture.startY;if(refreshGesture.mode==='pending'){if(Math.abs(dx)<DIRECTION_LOCK&&Math.abs(dy)<DIRECTION_LOCK)return;if(Math.abs(dx)>=Math.abs(dy)*1.15||dy<=0){refreshGesture.mode='cancelled';return}refreshGesture.mode='refresh'}if(refreshGesture.mode!=='refresh')return;if(!atTop()){refreshGesture=null;hideRefresh();return}refreshGesture.armed=revealRefresh(Math.max(0,dy));event.preventDefault();touchSwipe=null}function endRefreshGesture(touch){if(!refreshGesture||touch.identifier!==refreshGesture.id)return;const armed=refreshGesture.mode==='refresh'&&refreshGesture.armed;refreshGesture=null;if(!armed){hideRefresh();return}deck.classList.add('refreshing');deck.style.setProperty('--deck-pull',`${REFRESH_REVEAL}px`);refreshTab.textContent='Actualizando…';setTimeout(()=>{const url=new URL(window.location.href);url.searchParams.set('_refresh',Date.now().toString());window.location.replace(url.toString())},120)}document.addEventListener('touchstart',e=>{if(e.touches.length===1)beginRefreshGesture(e.touches[0],e.target)},{capture:true,passive:true});document.addEventListener('touchmove',e=>{if(!refreshGesture)return;const t=touchById(e.touches,refreshGesture.id);if(t)moveRefreshGesture(t,e)},{capture:true,passive:false});document.addEventListener('touchend',e=>{if(!refreshGesture)return;const t=touchById(e.changedTouches,refreshGesture.id);if(t)endRefreshGesture(t)},{capture:true,passive:true});document.addEventListener('touchcancel',()=>{refreshGesture=null;hideRefresh()},{capture:true,passive:true});

window.BoatStation={
  updateGPS:data=>modules.gps.update(data),
  updatePhone:data=>modules.phone.update(data),
  updateCompass:value=>modules.compass.update(value),
  updateMotion:value=>modules.seastate.update(value),
  updateBattery:data=>modules.batteries.updateBattery(data),
  bluetoothDevices:list=>{scanDevices=Array.isArray(list)?list:[];if(bluetoothScanner.classList.contains('open'))renderBluetoothScanner(scanDevices.length?`${scanDevices.length} dispositivo${scanDevices.length===1?'':'s'} encontrado${scanDevices.length===1?'':'s'}`:'Escaneando dispositivos Bluetooth…')}
};