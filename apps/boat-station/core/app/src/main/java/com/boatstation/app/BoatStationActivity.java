package com.boatstation.app;

/**
 * Production launcher. All native capabilities live in MainActivityCore and its hardware/storage
 * parents. The UI is loaded exactly once by MainActivityCore from the canonical PWA root.
 */
public class BoatStationActivity extends MainActivityCore {
}
