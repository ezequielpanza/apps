const playButton=document.getElementById('playButton');const stopButton=document.getElementById('stopButton');const playOverlay=document.getElementById('playOverlay');const roomStage=document.getElementById('roomStage');const playStageCopy=document.getElementById('playStageCopy');const appShell=document.getElementById('appShell');function startPlay(){playStageCopy.innerHTML='';const clone=roomStage.cloneNode(true);clone.removeAttribute('id');clone.querySelectorAll('.selected-object').forEach(el=>el.classList.remove('selected-object'));clone.querySelectorAll('.selection-label,.hotspot-door span').forEach(el=>el.remove());playStageCopy.appendChild(clone);playOverlay.classList.add('open');playOverlay.setAttribute('aria-hidden','false')}function stopPlay(){playOverlay.classList.remove('open');playOverlay.setAttribute('aria-hidden','true');playStageCopy.innerHTML=''}playButton.addEventListener('click',startPlay);stopButton.addEventListener('click',stopPlay);document.addEventListener('keydown',e=>{if(e.key==='Escape'&&playOverlay.classList.contains('open'))stopPlay()});document.querySelectorAll('.tree-item[data-room]').forEach(item=>item.addEventListener('click',()=>{document.querySelectorAll('.tree-item[data-room]').forEach(i=>i.classList.remove('active'));item.classList.add('active');document.querySelector('.context-pill').innerHTML='<span class="dot"></span> Room / '+item.dataset.room}));document.querySelectorAll('.mode-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.mode-tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active')}));document.querySelectorAll('.bottom-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.bottom-tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active')}));

const panelLimits={project:{min:170,max:420,storage:'adventureStudioProjectWidth',css:'--project-width'},inspector:{min:220,max:480,storage:'adventureStudioInspectorWidth',css:'--inspector-width'}};
function clamp(value,min,max){return Math.min(Math.max(value,min),max)}
function restorePanelWidths(){Object.values(panelLimits).forEach(panel=>{const stored=Number(localStorage.getItem(panel.storage));if(Number.isFinite(stored)&&stored>0){document.documentElement.style.setProperty(panel.css,`${clamp(stored,panel.min,panel.max)}px`)}})}
function setupResizer(elementId,side){const handle=document.getElementById(elementId);if(!handle)return;const panel=panelLimits[side];let startX=0;let startWidth=0;let pointerId=null;const onMove=e=>{if(pointerId===null)return;const delta=e.clientX-startX;const width=side==='project'?startWidth+delta:startWidth-delta;const next=clamp(width,panel.min,panel.max);document.documentElement.style.setProperty(panel.css,`${next}px`)};const finish=()=>{if(pointerId===null)return;pointerId=null;handle.classList.remove('dragging');document.body.classList.remove('resizing-panels');const current=parseFloat(getComputedStyle(document.documentElement).getPropertyValue(panel.css));localStorage.setItem(panel.storage,String(Math.round(current)));window.removeEventListener('pointermove',onMove);window.removeEventListener('pointerup',finish);window.removeEventListener('pointercancel',finish)};handle.addEventListener('pointerdown',e=>{pointerId=e.pointerId;startX=e.clientX;startWidth=side==='project'?document.querySelector('.sidebar').getBoundingClientRect().width:document.querySelector('.inspector').getBoundingClientRect().width;handle.classList.add('dragging');document.body.classList.add('resizing-panels');window.addEventListener('pointermove',onMove);window.addEventListener('pointerup',finish);window.addEventListener('pointercancel',finish)});handle.addEventListener('dblclick',()=>{const defaultWidth=side==='project'?232:286;document.documentElement.style.setProperty(panel.css,`${defaultWidth}px`);localStorage.removeItem(panel.storage)})}
restorePanelWidths();setupResizer('leftResizer','project');setupResizer('rightResizer','inspector');